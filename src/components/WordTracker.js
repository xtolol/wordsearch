import React from "react";
import { useState } from "react";

//Word
//props.word (From State array in app.js)

function WordTracker(props)  {
    return (
        <div class="tracker-container">
            
        </div>
    );
}